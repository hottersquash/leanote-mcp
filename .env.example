# MCP HTTP 服务（非敏感，可保留在 .env 或 docker-compose 中）
MCP_HOST=0.0.0.0
MCP_PORT=3100
LEANOTE_CONFIG_PATH=config/leanote.json

# Leanote 凭据请写入 config/leanote.json（见 config/leanote.example.json）
# 不要将账号密码放在 .env 或 docker-compose.yml 中
